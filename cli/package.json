{
  "name": "@codeviz/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Codeviz CLI — analyze a target project and emit SymbolGraph / IOEntryRegistry / FlowGraph / BusinessAnnotations JSON. M1.1 ships only the zod schemas; the analyzer/tracer/translator implementations land in M1.2 – M1.4.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p .",
    "validate:ground-truth": "tsx src/schemas/validate-ground-truth.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
